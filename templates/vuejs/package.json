{
  "name": "{{name}}",
  "description": "{{description}}",
  "version": "1.0.0",
  "author": "{{author}}",
  "license": "{{license}}",
  "main": "src/index.js",
  "scripts": {
    "dev": "freelog-cli-service serve --open",
    "build": "freelog-cli-service build"
  },
  "keywords": [],
  "devDependencies": {
    "@freelog/cli-service": "^0.1.7"
  },
  "dependencies": {
  }
}
